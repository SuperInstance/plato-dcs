[build-system]
requires = ["setuptools>=61.0", "wheel"]
build-backend = "setuptools.build_meta"
[project]
name = "plato-dcs"
version = "0.1.0"
description = "Dynamic Consensus System — multi-agent belief, lock accumulation, consensus"
license = {text = "MIT"}
requires-python = ">=3.8"
